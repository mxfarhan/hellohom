import 'package:foodly/model/chat.dart';
import 'package:scoped_model/scoped_model.dart';

mixin ChatService on Model {
  // List<Chat> _chatList = [];
  // List<Chat> get chatList => _chatList;

  // Future addToChat(Chat chat, String fromPage) async {
  //   _chatList.add(chat);
  //   if (fromPage == "SendMessage") {
  //     notifyListeners();
  //   }
  // }
}
